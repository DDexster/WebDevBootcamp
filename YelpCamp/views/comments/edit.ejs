<% include ../partials/header %>

<div class="container">
  <div class="row text-center">
    <h1>Edit Your Comment</h1>
    
   <div style="width:30%; margin:25px auto;">
      <form class="" action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
        <div class="form-group">
            <textarea class="form-control" 
                   placeholder="Your comment here..." 
                   name="comment[text]"
                   value=""><%= comment.text %></textarea>
        </div>

        <div class="form-group">
            <button class="btn btn-info btn-lg btn-block">Submit Сhanges</button>
        </div>
      </form>
      <form action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>/?_method=DELETE" method="POST">
            <button class="btn btn-danger btn-lg btn-block">Delete Comment</button>
      </form>
   </div>
    
    <a href="/campgrounds/<%=campground_id%>">Go back</a>
  </div>
</div>

<% include ../partials/footer %>
